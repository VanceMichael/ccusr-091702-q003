# 领域约定

无障碍设施整改中枢围绕公共设施缺陷聚合、责任认领和复验闭环保存业务事实。主要对象为辖区、设施版本、巡查批次、原始报告、整改周期、临时占用、复验记录。

必须长期遵守以下约束：相似报告可归入一个缺陷但原始陈述不可丢失；复发开启新周期；临时占用按批准时窗生效；关闭缺陷需要辖区和专业范围匹配。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。